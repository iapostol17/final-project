# Final Project 
# INFO 201 B

# ui.R
library(shiny)
library(plotly)
library(styler)
library(lintr)
library(ggplot2)

#
shinyUI(navbarPage( 
  title = "", 
  tabsetPanel(
    tabPanel(
      
    ), 
    tabPanel(
      
    ), 
    tabPanel(
      
    ), 
    tabPanel(
      
    )
  )
))
